# Application Configuration
spring.application.name=adventure-game

# Database - PostgreSQL (Render provides DATABASE_URL in postgres:// format)
# Spring Boot needs jdbc:postgresql:// format
spring.datasource.url=${JDBC_DATABASE_URL:${DATABASE_URL:jdbc:postgresql://localhost:5432/adventuredb}}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# JWT
app.jwt.secret=${JWT_SECRET:YourSuperSecretJWTKeyThatShouldBeAtLeast256BitsLongForHMACSHA256Algorithm}
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Server
server.port=${PORT:8080}

# Disable H2 Console
spring.h2.console.enabled=false

# Logging
logging.level.com.adventure=INFO

# JWT
app.jwt.secret=YourSuperSecretJWTKeyThatShouldBeAtLeast256BitsLongForHMACSHA256Algorithm
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# Server
server.port=8081

# Logging
logging.level.com.adventure=INFO

# PostgreSQL (Production) - Uncomment and configure for production
# spring.datasource.url=jdbc:postgresql://localhost:5432/adventuredb
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=your_db_user
# spring.datasource.password=your_db_password
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
